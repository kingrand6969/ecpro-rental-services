# Hosts like Render build with NODE_ENV=production, which makes npm skip
# devDependencies — but the build itself runs vite, esbuild and tsx, which
# live there. Install them regardless of NODE_ENV.
include=dev
